const initialState = {
    loading: false,
    resources: null,
    error: null
};

const resources = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};
export default resources;
